
let initialState = {
    friends: [
        { id:1, name: 'Max'},
        { id:2, name: 'Bork'},
        { id:3, name: 'Lind'}
    ]
};

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;