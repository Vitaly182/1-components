import preloader from '../../../assets/images/Spinner-5.gif'

let Preloader = (props) => {
    return (
        <div>
            <img src={preloader} />
        </div>
    )
}

export default Preloader;